name: library_sampler
description: Library sampler app to test out plugins
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  flutter_html: ^1.0.0-pre.1
  flutter_widget_from_html_core: ^0.3.3+4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true